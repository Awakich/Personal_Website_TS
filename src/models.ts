export interface RepoProps {
    name: string;
    id?: number;
    description: string;
    created_at: string;
    html_url: string;
    language: string;
}

export interface ImgProp {
    img: string;
}